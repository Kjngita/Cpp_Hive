#include "Serializer.hpp"

uintptr_t	Serializer::serialize(Data* ptr) {
	//takes a pointer and converts to unsigned integer type uintptr_t
}

Data*	Serializer::deserialize(uintptr_t raw) {
	//takes an unsigned integer parameter and converts to a pointer to Data
}